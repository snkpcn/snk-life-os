# Public — safe to expose client-side, protected by Supabase RLS
NEXT_PUBLIC_SUPABASE_URL=https://pbbihfipfbpiqbiqlagd.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Set to "true" only after SMTP is configured and verified in Supabase Auth settings
NEXT_PUBLIC_ENABLE_MAGIC_LINK=false

# Server-only secrets — set these in Vercel project settings, never commit real values
ANTHROPIC_API_KEY=
